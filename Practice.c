#include <stdio.h>
int main() {
    float m1 = 20; // math marks 
    float m2 = 30; // physics marks
    float m3 = 40; // chemistry marks 
    float m4 = 25; // english marks 
    float m5 = 10; // economics marks 
    float p = (m1 + m2 + m3 + m4 + m5) / 200 * 100; // percentage of 4 subjects where marks are out of 40



    printf("%f", p);





    return 0;
}